{
  "name": "sistema-universidade-web2",
  "version": "1.0.0",
  "description": "API do Sistema Universitário Web2 (Node.js + JavaScript)",
  "main": "src/server.js",
  "license": "ISC",
  "type": "module",
  "scripts": {
    "dev": "cross-env DATABASE_URL=\"postgresql://postgres:postgres@localhost:5432/universidade?schema=public\" nodemon src/server.js",
    "start": "node src/server.js",
    "prisma": "npx prisma",
    "prisma:migrate": "cross-env DATABASE_URL=\"postgresql://postgres:postgres@localhost:5432/universidade?schema=public\" npx prisma migrate dev",
    "prisma:studio": "cross-env DATABASE_URL=\"postgresql://postgres:postgres@localhost:5432/universidade?schema=public\" npx prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^4.16.2",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "prisma": "^4.16.2"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "nodemon": "^3.1.0"
  }
}
